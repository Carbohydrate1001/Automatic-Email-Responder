# 开发日志

> 项目：Automatic-Email-Responder
> 维护目标：记录每次更新新增的功能与修复的 Bug。

---

## 2026-03-09

### 新增功能
1. **邮件发送失败识别与重试机制**
   - 新增发送重试配置项：`SEND_RETRY_MAX_ATTEMPTS`、`SEND_RETRY_DELAY_SECONDS`。
   - Graph 发信流程支持自动重试，失败后抛出明确异常。
   - 自动发送与人工发送流程均接入重试逻辑。

2. **失败状态可观测**
   - 增加状态：`send_failed`（发送失败，可重试）。
   - 邮件记录增加字段：`retry_count`、`last_error`，用于追踪失败次数与原因。
   - 列表/详情/统计页同步展示失败状态与数量。

3. **非业务邮件识别与自动忽略**
   - 分类新增：`non_business`（非业务邮件，无需回复）。
   - 通过提示词规则强化 + 规则兜底（如通知/营销/系统消息）降低误分类。
   - 对 `non_business` 邮件不生成回复、不发送，状态置为 `ignored_no_reply`。

4. **仓库忽略规则完善**
   - 新增 `backend/.gitignore`，忽略 `backend/.env`。
   - 已将 `backend/.env` 从 Git 跟踪移除（本地文件保留）。

### Bug 修复
1. **发送故障无识别、无重试**
   - 问题：邮件发送失败后系统无法感知，流程中断且无可追踪信息。
   - 修复：补齐错误捕获、失败状态写入、重试机制与失败详情回传。

2. **非业务内容被高置信度误分为业务并自动回复**
   - 问题：如 OneDrive 通知类邮件被误分到 `order_tracking` 并自动发送回复。
   - 修复：新增 `non_business` 类别及规则过滤，并将该类邮件自动忽略。

---

## 2026-03-10

### 新增功能
1. **拉取流程稳健性增强（逐封隔离）**
   - `POST /api/fetch` 改为逐封 `try/except`，单封失败不再中断整批处理。
   - 新增返回字段：`failed`、`errors`、`status_breakdown`，便于定位失败邮件。

2. **两阶段分类判定（业务相关性 Gate + 业务类别分类）**
   - 新增业务相关性判定步骤：先判断邮件是否属于业务服务请求，再执行具体类别分类。
   - 对判定为非业务的邮件直接归类 `non_business`，减少“关键词误判为业务”的概率。

3. **自动发送策略收紧**
   - 自动发送增加安全门槛：必须业务相关且满足阈值条件，进一步降低误发风险。
   - 非业务邮件统一标记为 `ignored_no_reply`，不生成、不发送回复。

4. **可观测性指标增强**
   - 增加结构化流水日志（`message_id`、`status`、`category`、`confidence`、`latency_ms`）。
   - 拉取接口返回处理时延指标：`total_elapsed_ms`、`avg_per_email_ms`。
   - 统计接口新增 `non_business_rate`、`send_failure_rate`。

### Bug 修复
1. **批处理中断问题**
   - 问题：任意一封邮件处理异常会导致本次批量拉取整体失败。
   - 修复：改为逐封隔离处理并记录失败明细，保证其余邮件继续处理。

2. **误分类导致误发送风险仍偏高**
   - 问题：部分非业务邮件在关键词干扰下仍可能进入业务分类并触发自动发送。
   - 修复：引入业务相关性 Gate，并收紧自动发送条件，优先将非业务邮件落入 `ignored_no_reply`。

---

## 2026-03-12

### 新增功能
1. **新增完整自动回复模板体系（覆盖全部核心分类）**
   - 统一落地模板类别：`pricing_inquiry`、`order_cancellation`、`order_tracking`、`shipping_time`、`shipping_exception`、`billing_invoice`、`non_business`。
   - `billing_invoice` 使用固定格式财务回函模板：付款状态固定为“已付款”，账单号/账单金额/发票信息随机生成。
   - `non_business` 使用固定说明模板：明确告知该邮件不涉及进一步业务回复。
   - 模板客户称呼统一使用发件人邮箱前缀（去除 `@` 后缀部分），并完成类别分发接入。
   - 涉及模块：`backend/services/reply_service.py`。

2. **非业务邮件回复策略调整**
   - 非业务邮件仍保持 `ignored_no_reply` 状态且不自动发送。
   - 由“空回复”调整为“生成固定说明模板并入库”，便于前端详情页展示与审计追溯。
   - 涉及模块：`backend/services/reply_service.py`。

3. **数据系统新增（公司信息库）**
   - 新增公司产品信息数据层，支持从 `JSON` 文件读取/校验/新增/更新/删除产品数据。
   - 新增公司信息管理接口：`/api/company/products`（`GET/PUT/POST/PATCH/DELETE`），用于维护产品目录并为模板生成提供数据支撑。
   - 新增数据文件：`backend/data/company_products.json`。
   - 涉及模块：`backend/services/company_info_service.py`、`backend/routes/company_routes.py`、`backend/data/company_products.json`。

4. **文档同步更新**
   - 更新 `non_business` 类别说明与处理流程文案，明确“生成固定说明模板但不自动发送”。
   - 涉及模块：`使用说明.md`。

### Bug 修复
1. **`billing_invoice` 类别缺少模板导致回退到通用 GPT 生成**
   - 问题：账单/发票类邮件未走模板路径，回复格式不稳定。
   - 修复：新增 `billing_invoice` 专用模板并接入类别分发逻辑。

---


## TODO List

1. [ ] **梳理现有分类手段（当前已实现）**
   - 规则兜底：基于 `BUSINESS_HINTS` 与 `NON_BUSINESS_HINTS` 先过滤明显的非业务邮件。
   - 两阶段分类：先做业务相关性 Gate，再在 7 个既定类别中进行细分。
   - Prompt 约束：通过固定类别定义、负面约束和 `JSON` 结构化输出降低误判与解析风险。
   - 结果后处理：对非法类别回退到 `non_business`，并结合规则结果对高置信度误判进行纠偏。
   - 发送侧保护：分类后仍通过置信度阈值控制自动发送，低置信度邮件转人工审核。

2. [ ] **继续加强分类能力与安全策略**
   - 增加 `few-shot` 示例与反例样本，进一步缩小相近类别之间的判断边界。
   - 增加人工反馈/偏好采集机制，用于持续迭代分类提示词与回复提示词。
   - 针对易混淆场景增加二次判别逻辑，例如“业务相关”与“非业务通知”的再校验。
   - 为不同类别设置更细粒度的自动发送阈值，而不是仅依赖统一阈值。
   - 在 `OpenAI API` 或 `Microsoft Graph API` 异常时自动暂停回复流程，并等待人工介入。
   - 补充更多审计与异常告警机制，提升系统稳健性与可追溯性。

---

## 后续维护约定（简版）

- 每次更新按日期新增一节，包含：
  1. **新增功能**（面向用户可感知能力）
  2. **Bug 修复**（问题现象 + 修复方式）
- 建议附上涉及模块（如 `backend/services/*`、`frontend/src/views/*`）以便追溯。
